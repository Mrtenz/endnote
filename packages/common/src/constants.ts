/**
 * Valid values for the `deleteAfter` fields. These values are the time in days after which a note should be deleted.
 */
export const DELETE_AFTER_VALUES = [1, 7, 30];

/**
 * Milliseconds in a day.
 */
export const DAY_IN_MILLISECONDS = 86400000;
